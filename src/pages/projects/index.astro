---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { breadcrumbListJsonLd } from '../../lib/seo/structuredData';

const projects = await getCollection('projectsIt');
const slugFromId = (id: string) => id.replace(/^(it|en)\//, '');

const sortedProjects = [...projects].sort((a, b) => {
  // featured first
  if (a.data.featured !== b.data.featured) return a.data.featured ? -1 : 1;
  // then order asc
  if (a.data.order !== b.data.order) return a.data.order - b.data.order;
  // then title
  return a.data.title.localeCompare(b.data.title);
});

const statusLabel: Record<string, string> = {
  active: 'Active',
  beta: 'Beta',
  archived: 'Archived',
};

const site = Astro.site ?? new URL('https://devisia.pro');
const breadcrumbs = breadcrumbListJsonLd({
  site,
  items: [
    { name: 'Home', url: '/' },
    { name: 'Projects', url: '/projects' },
  ],
});
---

<Layout
  title="Progetti"
  description="Scopri prodotti e progetti Devisia: ciò che stiamo costruendo, sperimentando e portando in produzione."
  structuredData={[breadcrumbs]}
>
  <div class="projects-container">
    <header class="projects-header">
      <h1>Progetti</h1>
      <p class="projects-intro">
        Una selezione di prodotti e iniziative: dalle prime sperimentazioni ai progetti attivi.
      </p>
    </header>

    {sortedProjects.length === 0 ? (
      <p class="empty">Nessun progetto disponibile.</p>
    ) : (
      <div class="grid">
        {sortedProjects.map((project) => (
          <article class="card">
            <div class="card-top">
              <div class="media">
                {project.data.logo ? (
                  <img class="logo" src={project.data.logo} alt={`${project.data.title} logo`} />
                ) : null}

                <div class="meta">
                  <div class="title-row">
                    <h2 class="title">{project.data.title}</h2>
                    <span class={`status status--${project.data.status}`}>
                      {statusLabel[project.data.status] ?? project.data.status}
                    </span>
                  </div>
                  <p class="desc">{project.data.description}</p>
                </div>
              </div>
            </div>

            {project.data.coverImage ? (
              <div class="cover">
                <img
                  src={project.data.coverImage}
                  alt={`${project.data.title} cover`}
                  loading="lazy"
                />
              </div>
            ) : null}

            <div class="card-bottom">
              {project.data.tags?.length ? (
                <div class="tags">
                  {project.data.tags.map((tag) => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              ) : null}

              <div class="actions">
                <a class="cta" href={`/projects/${slugFromId(project.id)}`}>Vedi progetto</a>
              </div>
            </div>
          </article>
        ))}
      </div>
    )}

    <section class="page-intro" aria-label="Introduzione ai progetti">
      <details class="intro-details">
        <summary class="intro-summary">
          <span class="intro-summary-title">Cosa significa “progetto” per noi</span>
          <svg class="intro-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </summary>
        <div class="intro-body">
          <h2 class="intro-title">Cosa significa “progetto” per noi</h2>
          <p class="intro-text">
            In questa pagina raccogliamo i prodotti e le iniziative su cui lavoriamo: alcuni sono “case study” con un obiettivo
            chiaro e una roadmap definita, altri sono sperimentazioni controllate che ci aiutano a validare idee, pattern tecnici o
            approcci di delivery. In entrambi i casi, il filo conduttore è lo stesso: costruire software che crea valore misurabile,
            mantenendo la complessità sotto controllo e rispettando requisiti di sicurezza e privacy.
          </p>
          <p class="intro-text">
            Ogni card qui sotto ti dà un’idea rapida del contesto (descrizione, stato, tag). Lo “stato” non è solo una label estetica:
            ci aiuta a capire quanto un progetto sia vicino alla produzione e che tipo di rischio residuo aspettarsi. “Active” indica
            un prodotto o iniziativa in uso o in rollout; “Beta” è un lavoro in validazione; “Archived” è qualcosa che abbiamo concluso
            o messo in pausa, spesso perché abbiamo imparato abbastanza e il valore marginale di continuare era basso.
          </p>

          <div class="intro-grid" role="list">
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">Come lavoriamo</h3>
              <ul class="intro-list">
                <li><strong>Discovery breve</strong>: obiettivi, utenti, vincoli, dati, integrazioni.</li>
                <li><strong>Prototipo / MVP</strong>: validazione rapida con feedback reali.</li>
                <li><strong>Industrializzazione</strong>: qualità, test, osservabilità, CI/CD, hardening.</li>
                <li><strong>Iterazione</strong>: misurazione, miglioramento continuo, roadmap.</li>
              </ul>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">Cosa puoi aspettarti</h3>
              <p class="intro-item-text">
                Troverai descrizioni sintetiche, focus su risultati e “perché” delle scelte. Quando un progetto include AI, ci
                concentriamo anche su governance, audit logging, minimizzazione dati e controlli policy per ridurre rischio e sorprese.
                Se ti interessa questa parte, puoi partire dalla nostra
                <a class="intro-link" href="/tools/ai-risk-checklist">AI Risk &amp; Privacy Checklist</a>.
              </p>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">Collaborazioni</h3>
              <p class="intro-item-text">
                Se vuoi capire come un progetto simile potrebbe applicarsi alla tua realtà, scrivici: bastano obiettivo, contesto e
                vincoli principali. Possiamo valutare insieme se ha senso un audit rapido, un prototipo o un percorso di sviluppo.
                Vai alla pagina <a class="intro-link" href="/contact">Contatti</a>.
              </p>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">FAQ rapida</h3>
              <p class="intro-item-text">
                <strong>Perché alcuni progetti sono archiviati?</strong> Perché abbiamo già estratto valore (apprendimento, pattern,
                riuso) o perché le priorità sono cambiate. Archiviare è spesso una scelta di focus, non un “fallimento”.
              </p>
              <p class="intro-item-text">
                <strong>Posso vedere dettagli tecnici?</strong> Quando possibile li includiamo nelle pagine di progetto e nel blog.
              </p>
            </div>
          </div>

          <p class="intro-text intro-note">
            Nota: questa pagina evolve nel tempo. Se vuoi una panoramica completa dei servizi, visita
            <a class="intro-link" href="/services">Servizi</a>.
          </p>
        </div>
      </details>
    </section>
  </div>
</Layout>

<style>
  .projects-container {
    max-width: 1000px;
    margin: 0 auto;
  }

  .projects-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .projects-header h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .projects-intro {
    font-size: 1.2rem;
    color: var(--ds-color-text-muted);
    max-width: 700px;
    margin: 0 auto;
  }

  .page-intro {
    margin: 2.5rem auto 0;
    max-width: 900px;
  }

  .intro-details {
    background: var(--ds-color-surface);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-3);
    padding: var(--ds-space-4);
  }

  .intro-details[open] {
    padding: var(--ds-space-6);
  }

  .intro-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--ds-space-4);
    cursor: pointer;
    list-style: none;
  }

  .intro-summary-title {
    font-weight: 700;
    color: var(--ds-color-text);
    font-size: var(--ds-text-lg);
  }

  .intro-summary::-webkit-details-marker {
    display: none;
  }

  .intro-summary:focus-visible {
    outline: none;
    box-shadow: var(--ds-focus-ring);
    border-radius: var(--ds-radius-2);
  }

  .intro-chevron {
    color: var(--ds-color-text-muted);
    flex: 0 0 auto;
    transition: transform var(--ds-motion-base) var(--ds-ease-standard);
  }

  .intro-details[open] .intro-chevron {
    transform: rotate(180deg);
  }

  .intro-body {
    margin-top: var(--ds-space-4);
  }

  .intro-title {
    margin: 0;
    color: var(--ds-color-text);
    font-size: var(--ds-text-xl);
    font-weight: 700;
  }

  .intro-text {
    margin: 0 0 var(--ds-space-3);
    color: var(--ds-color-text);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-note {
    margin-top: var(--ds-space-4);
    color: var(--ds-color-text-muted);
  }

  .intro-link {
    color: var(--ds-color-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--ds-space-5);
    margin: var(--ds-space-5) 0 var(--ds-space-4);
  }

  .intro-item {
    background: var(--ds-color-surface-2);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-2);
    padding: var(--ds-space-4);
  }

  .intro-item-title {
    margin: 0 0 var(--ds-space-2);
    color: var(--ds-color-text);
    font-size: var(--ds-text-md);
    font-weight: 700;
  }

  .intro-item-text {
    margin: 0 0 var(--ds-space-2);
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-list {
    margin: 0;
    padding-left: 1.15rem;
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-list li + li {
    margin-top: 0.35rem;
  }

  @media (max-width: 768px) {
    .intro-details {
      padding: var(--ds-space-4);
    }

    .intro-details[open] {
      padding: var(--ds-space-5);
    }

    .intro-grid {
      grid-template-columns: 1fr;
    }
  }

  .empty {
    text-align: center;
    padding: 3rem 1rem;
    color: var(--ds-color-text-muted);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2rem;
  }

  .card {
    background: var(--ds-color-surface);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-3);
    overflow: hidden;
    transition: transform 0.2s, box-shadow 0.2s;
    display: flex;
    flex-direction: column;
  }

  .card:hover {
    transform: translateY(-4px);
    box-shadow: var(--ds-shadow-2);
  }

  .card-top {
    padding: 1.25rem 1.25rem 0;
  }

  .media {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .logo {
    width: 44px;
    height: 44px;
    object-fit: contain;
    border-radius: var(--ds-radius-2);
    border: var(--ds-border-1) solid var(--ds-color-border);
    background: var(--ds-color-surface-2);
    padding: 0.25rem;
    flex: 0 0 auto;
  }

  .meta {
    flex: 1;
    min-width: 0;
  }

  .title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .title {
    margin: 0;
    font-size: 1.35rem;
    line-height: 1.25;
    color: var(--ds-color-text);
  }

  .desc {
    margin: 0;
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .status {
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.25rem 0.6rem;
    border-radius: var(--ds-radius-round);
    border: var(--ds-border-1) solid var(--ds-color-border);
    white-space: nowrap;
  }
  .status--active {
    color: var(--ds-color-success);
    background: color-mix(in srgb, var(--ds-color-success) 12%, var(--ds-color-surface));
    border-color: color-mix(in srgb, var(--ds-color-success) 35%, var(--ds-color-border));
  }
  .status--beta {
    color: var(--ds-color-info);
    background: color-mix(in srgb, var(--ds-color-info) 12%, var(--ds-color-surface));
    border-color: color-mix(in srgb, var(--ds-color-info) 35%, var(--ds-color-border));
  }
  .status--archived {
    color: var(--ds-color-text-muted);
    background: var(--ds-color-surface-2);
    border-color: var(--ds-color-border);
  }

  .cover {
    width: 100%;
    height: 180px;
    margin-top: 1rem;
    background: var(--ds-color-surface-2);
    border-top: var(--ds-border-1) solid var(--ds-color-border);
    border-bottom: var(--ds-border-1) solid var(--ds-color-border);
    overflow: hidden;
  }

  .cover img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .card-bottom {
    padding: 1rem 1.25rem 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: auto;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background-color: color-mix(in srgb, var(--ds-color-accent) 15%, var(--ds-color-surface-2));
    color: var(--ds-color-accent);
    border-radius: var(--ds-radius-1);
    font-size: 0.875rem;
    font-weight: 500;
    border: var(--ds-border-1) solid color-mix(in srgb, var(--ds-color-accent) 30%, var(--ds-color-border));
  }

  .actions {
    display: flex;
    justify-content: flex-end;
  }

  .cta {
    color: var(--ds-color-accent);
    text-decoration: none;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    transition: gap var(--ds-motion-base);
  }

  .cta:hover {
    gap: 0.5rem;
  }

  @media (max-width: 768px) {
    .projects-header h1 {
      font-size: 2rem;
    }
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>

