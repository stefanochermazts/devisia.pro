---
import Layout from '../../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { breadcrumbListJsonLd } from '../../../lib/seo/structuredData';

const projects = await getCollection('projectsEn');
const slugFromId = (id: string) => id.replace(/^(it|en)\//, '');

const sortedProjects = [...projects].sort((a, b) => {
  if (a.data.featured !== b.data.featured) return a.data.featured ? -1 : 1;
  if (a.data.order !== b.data.order) return a.data.order - b.data.order;
  return a.data.title.localeCompare(b.data.title);
});

const statusLabel: Record<string, string> = {
  active: 'Active',
  beta: 'Beta',
  archived: 'Archived',
};

const site = Astro.site ?? new URL('https://devisia.pro');
const breadcrumbs = breadcrumbListJsonLd({
  site,
  items: [
    { name: 'Home', url: '/en/' },
    { name: 'Projects', url: '/en/projects' },
  ],
});
---

<Layout
  title="Projects"
  description="Explore Devisia projects and products—what we’re building, experimenting with, and shipping."
  lang="en"
  structuredData={[breadcrumbs]}
>
  <div class="projects-container">
    <header class="projects-header">
      <h1>Projects</h1>
      <p class="projects-intro">
        A curated list of products and initiatives—from early experiments to active launches.
      </p>
    </header>

    {sortedProjects.length === 0 ? (
      <p class="empty">No projects yet.</p>
    ) : (
      <div class="grid">
        {sortedProjects.map((project) => (
          <article class="card">
            <div class="card-top">
              <div class="media">
                {project.data.logo ? (
                  <img class="logo" src={project.data.logo} alt={`${project.data.title} logo`} />
                ) : null}

                <div class="meta">
                  <div class="title-row">
                    <h2 class="title">{project.data.title}</h2>
                    <span class={`status status--${project.data.status}`}>
                      {statusLabel[project.data.status] ?? project.data.status}
                    </span>
                  </div>
                  <p class="desc">{project.data.description}</p>
                </div>
              </div>
            </div>

            {project.data.coverImage ? (
              <div class="cover">
                <img
                  src={project.data.coverImage}
                  alt={`${project.data.title} cover`}
                  loading="lazy"
                />
              </div>
            ) : null}

            <div class="card-bottom">
              {project.data.tags?.length ? (
                <div class="tags">
                  {project.data.tags.map((tag) => (
                    <span class="tag">{tag}</span>
                  ))}
                </div>
              ) : null}

              <div class="actions">
                <a class="cta" href={`/en/projects/${slugFromId(project.id)}`}>View project</a>
              </div>
            </div>
          </article>
        ))}
      </div>
    )}

    <section class="page-intro" aria-label="Projects introduction">
      <details class="intro-details">
        <summary class="intro-summary">
          <span class="intro-summary-title">What “project” means to us</span>
          <svg class="intro-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </summary>
        <div class="intro-body">
          <h2 class="intro-title">What “project” means to us</h2>
          <p class="intro-text">
            This page collects products and initiatives we work on. Some are full case studies with a clear goal and roadmap;
            others are controlled experiments used to validate ideas, technical patterns, or delivery approaches. In both cases,
            the goal is the same: build software that creates measurable value, keeps complexity manageable, and respects security
            and privacy requirements.
          </p>
          <p class="intro-text">
            Each card below gives you a quick snapshot (description, status, tags). The “status” is more than a label: it indicates
            how close the work is to production and what kind of residual risk to expect. “Active” means in use or rolling out; “Beta”
            means in validation; “Archived” means finished or paused—often because we learned what we needed and shifting focus was the
            rational choice.
          </p>

          <div class="intro-grid" role="list">
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">How we work</h3>
              <ul class="intro-list">
                <li><strong>Short discovery</strong>: goals, users, constraints, data, integrations.</li>
                <li><strong>Prototype / MVP</strong>: validate assumptions with real feedback.</li>
                <li><strong>Industrialization</strong>: quality, tests, observability, CI/CD, hardening.</li>
                <li><strong>Iteration</strong>: measurement, continuous improvement, roadmap.</li>
              </ul>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">What you can expect</h3>
              <p class="intro-item-text">
                You’ll find concise descriptions, a focus on outcomes, and the “why” behind decisions. When a project includes AI, we
                also emphasize governance, audit logging, data minimization, and policy checks to reduce surprises. If that’s relevant,
                start with our <a class="intro-link" href="/en/tools/ai-risk-checklist">AI Risk &amp; Privacy Checklist</a>.
              </p>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">Collaboration</h3>
              <p class="intro-item-text">
                If you want to evaluate how a similar approach could apply to your business, send us a note with your goal, context,
                and constraints. We can recommend next steps: a quick audit, a prototype, or a delivery roadmap. See
                <a class="intro-link" href="/en/contact">Contact</a>.
              </p>
            </div>
            <div class="intro-item" role="listitem">
              <h3 class="intro-item-title">Quick FAQ</h3>
              <p class="intro-item-text">
                <strong>Why are some projects archived?</strong> Because we extracted the value (learning, patterns, reusable components)
                or priorities changed. Archiving is often a focus decision, not a failure.
              </p>
              <p class="intro-item-text">
                <strong>Can I see technical details?</strong> When possible, we include them in project pages and blog posts.
              </p>
            </div>
          </div>

          <p class="intro-text intro-note">
            Note: this page evolves over time. For a complete overview of our offering, visit
            <a class="intro-link" href="/en/services">Services</a>.
          </p>
        </div>
      </details>
    </section>
  </div>
</Layout>

<style>
  .projects-container {
    max-width: 98vw;
    margin: 0 auto;
  }

  @media (min-width: 48rem) {
    .projects-container {
      max-width: 90vw;
    }
  }

  .projects-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .projects-header h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .projects-intro {
    font-size: 1.2rem;
    color: var(--ds-color-text-muted);
    max-width: 98vw;
    margin: 0 auto;
  }

  .page-intro {
    margin: 2.5rem auto 0;
    max-width: 98vw;
  }

  @media (min-width: 48rem) {
    .projects-intro,
    .page-intro {
      max-width: 90vw;
    }
  }

  .intro-details {
    background: var(--ds-color-surface);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-3);
    padding: var(--ds-space-4);
  }

  .intro-details[open] {
    padding: var(--ds-space-6);
  }

  .intro-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--ds-space-4);
    cursor: pointer;
    list-style: none;
  }

  .intro-summary-title {
    font-weight: 700;
    color: var(--ds-color-text);
    font-size: var(--ds-text-lg);
  }

  .intro-summary::-webkit-details-marker {
    display: none;
  }

  .intro-summary:focus-visible {
    outline: none;
    box-shadow: var(--ds-focus-ring);
    border-radius: var(--ds-radius-2);
  }

  .intro-chevron {
    color: var(--ds-color-text-muted);
    flex: 0 0 auto;
    transition: transform var(--ds-motion-base) var(--ds-ease-standard);
  }

  .intro-details[open] .intro-chevron {
    transform: rotate(180deg);
  }

  .intro-body {
    margin-top: var(--ds-space-4);
  }

  .intro-title {
    margin: 0;
    color: var(--ds-color-text);
    font-size: var(--ds-text-xl);
    font-weight: 700;
  }

  .intro-text {
    margin: 0 0 var(--ds-space-3);
    color: var(--ds-color-text);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-note {
    margin-top: var(--ds-space-4);
    color: var(--ds-color-text-muted);
  }

  .intro-link {
    color: var(--ds-color-accent);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .intro-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--ds-space-5);
    margin: var(--ds-space-5) 0 var(--ds-space-4);
  }

  .intro-item {
    background: var(--ds-color-surface-2);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-2);
    padding: var(--ds-space-4);
  }

  .intro-item-title {
    margin: 0 0 var(--ds-space-2);
    color: var(--ds-color-text);
    font-size: var(--ds-text-md);
    font-weight: 700;
  }

  .intro-item-text {
    margin: 0 0 var(--ds-space-2);
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-list {
    margin: 0;
    padding-left: 1.15rem;
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .intro-list li + li {
    margin-top: 0.35rem;
  }

  @media (max-width: 768px) {
    .intro-details {
      padding: var(--ds-space-4);
    }

    .intro-details[open] {
      padding: var(--ds-space-5);
    }

    .intro-grid {
      grid-template-columns: 1fr;
    }
  }

  .empty {
    text-align: center;
    padding: 3rem 1rem;
    color: var(--ds-color-text-muted);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2rem;
  }

  .card {
    position: relative;
    background: var(--ds-color-surface);
    border: var(--ds-border-1) solid var(--ds-color-border);
    border-radius: var(--ds-radius-3);
    overflow: hidden;
    transition: transform var(--ds-motion-base), box-shadow var(--ds-motion-base);
    display: flex;
    flex-direction: column;
  }

  .card:hover {
    transform: translateY(-4px);
    box-shadow: var(--ds-shadow-2);
  }

  .card-top {
    padding: 1.25rem 1.25rem 0;
  }

  .media {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
  }

  .logo {
    width: 44px;
    height: 44px;
    object-fit: contain;
    border-radius: var(--ds-radius-2);
    border: var(--ds-border-1) solid var(--ds-color-border);
    background: var(--ds-color-surface-2);
    padding: 0.25rem;
    flex: 0 0 auto;
  }

  .meta {
    flex: 1;
    min-width: 0;
  }

  .title-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.5rem;
  }

  .title {
    margin: 0;
    font-size: 1.35rem;
    line-height: 1.25;
    color: var(--ds-color-text);
  }

  .desc {
    margin: 0;
    color: var(--ds-color-text-muted);
    line-height: var(--ds-leading-relaxed);
  }

  .status {
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.25rem 0.6rem;
    border-radius: var(--ds-radius-round);
    border: var(--ds-border-1) solid var(--ds-color-border);
    white-space: nowrap;
  }
  .status--active {
    color: var(--ds-color-success);
    background: color-mix(in srgb, var(--ds-color-success) 12%, var(--ds-color-surface));
    border-color: color-mix(in srgb, var(--ds-color-success) 35%, var(--ds-color-border));
  }
  .status--beta {
    color: var(--ds-color-info);
    background: color-mix(in srgb, var(--ds-color-info) 12%, var(--ds-color-surface));
    border-color: color-mix(in srgb, var(--ds-color-info) 35%, var(--ds-color-border));
  }
  .status--archived {
    color: var(--ds-color-text-muted);
    background: var(--ds-color-surface-2);
    border-color: var(--ds-color-border);
  }

  .cover {
    width: 100%;
    height: 180px;
    margin-top: 1rem;
    background: var(--ds-color-surface-2);
    border-top: var(--ds-border-1) solid var(--ds-color-border);
    border-bottom: var(--ds-border-1) solid var(--ds-color-border);
    overflow: hidden;
  }

  .cover img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .card-bottom {
    padding: 1rem 1.25rem 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: auto;
  }

  .tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    display: inline-block;
    padding: 0.25rem 0.75rem;
    background-color: color-mix(in srgb, var(--ds-color-accent) 15%, var(--ds-color-surface-2));
    color: var(--ds-color-accent);
    border-radius: var(--ds-radius-1);
    font-size: 0.875rem;
    font-weight: 500;
    border: var(--ds-border-1) solid color-mix(in srgb, var(--ds-color-accent) 30%, var(--ds-color-border));
  }

  .actions {
    display: flex;
    justify-content: flex-end;
  }

  .cta {
    color: var(--ds-color-accent);
    text-decoration: none;
    font-weight: 700;
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    transition: gap var(--ds-motion-base);
  }

  .cta::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .cta:hover {
    gap: 0.5rem;
  }

  @media (max-width: 768px) {
    .projects-header h1 {
      font-size: 2rem;
    }
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>

