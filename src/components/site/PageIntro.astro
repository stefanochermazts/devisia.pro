---
export interface Props {
  eyebrow?: string;
  h1: string;
  lede: string;
}
const { eyebrow, h1, lede } = Astro.props;
---

<header class="site-pg-hero page-intro">
  {eyebrow && <p class="site-eyebrow">{eyebrow}</p>}
  <h1>{h1}</h1>
  <p class="page-intro__lede ds-muted">{lede}</p>
</header>
